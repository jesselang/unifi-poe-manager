[project]
name = "unifi-poe-manager"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "aiohttp",
    "aiounifi",
    "apscheduler",
    "sqlalchemy",
    "python-dotenv",
]

[project.scripts]
unifi-poe-manager = "unifi_poe_manager.cli:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
pythonpath = ["src"]
